## 🛠️ Требования

  * |Python 3.10+|

## 🚀 Установка и Запуск

### 1\. Клонирование репозитория

```bash
git clone https://github.com/Shirinshoh7/product-cli
cd product-cli
```

### 2\. Формат файла данных

Файл, с которым работает утилита (например, `products.txt`), должен содержать строки в следующем формате:

> **`Наименование — Цена`**

**Пример `products.txt`:**

```
btc - 50
eth - 500
sol - 1000
```

### 3\. Формат команды

Все команды выполняются в формате:

```bash
python main.py <имя_файла> <действие> <"Наименование"> [Цена]
```

## 📌 Примеры использования команд

### Добавление и Редактирование (`add`, `edit`)

| Действие | Описание | Пример Команды |
| :--- | :--- | :--- |
| `add` | Добавить новый продукт в список. | `python main.py products.txt add "товар" цена` |
| `edit` | Изменить цену существующего продукта. | `python main.py products.txt edit "товар" цена` |

### Удаление и Расчет (`delete`, `sum`)

| Действие | Описание | Пример Команды |
| :--- | :--- | :--- |
| `delete` | Удалить продукт по имени. | `python main.py products.txt delete "товар"` |
| `sum` | Посчитать общую сумму всех цен в списке. | `python main.py products.txt sum` |

-----

## 👤 Автор

**Shirinshoh Badalov**.

